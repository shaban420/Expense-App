# Git ignore — these files are NOT uploaded to GitHub

# Environment variables (contains secrets — NEVER upload)
.env
backend/.env
frontend/.env

# Node modules (too large — npm install recreates them)
node_modules/
backend/node_modules/
frontend/node_modules/

# React build output
frontend/build/

# System files
.DS_Store
Thumbs.db
